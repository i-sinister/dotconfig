compiler dotnet
iabbrev <buffer> cawait ConfigureAwait(false);
" save files before executin make and some other commands
set autowrite
" automatically reload changed files if corresponding havent changed
set autoread
